// Port numbers for the client, server, and docs
export const PORT_CLIENT = 8000;
export const PORT_SERVER = 4000;

export const BACKEND_URL = `http://localhost:${PORT_SERVER}`;
export const FRONTEND_URL = `http://localhost:${PORT_CLIENT}`;